import { ORIGIN } from "../../config.js"

export const chooseUser = (event) => {
const userId = event.target.form.userId.value;

const root = document.getElementById('root')
root.innerHTML = '';

try {
    const fetchUserById = 
} catch{}
}